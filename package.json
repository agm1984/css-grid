{
  "name": "css-grid",
  "version": "1.0.0",
  "description": "Wes Bos course",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --files '**/*.css, **/*.html, **/*.js' --directory --port 7777 --browser 'Chrome'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "css-grid"
  ],
  "author": "Adam Mackintosh",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.18.13"
  }
}
